import React from "react";

import "./Spinner.css";

const spinner = () => (
  <div
    style={{
      minHeight: "70vh",
    }}
  >
    <div className="Loader">Loading...</div>
  </div>
);

export default spinner;
